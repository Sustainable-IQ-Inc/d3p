# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these from your Supabase project settings
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE=your_service_role_key_here

# =============================================================================
# ENCRYPTION KEYS
# =============================================================================
# Generate secure random keys for local development
# You can use: openssl rand -base64 32 for ENCRYPTION_KEY
# You can use: openssl rand -base64 16 for ENCRYPTION_SALT
ENCRYPTION_KEY=your_32_character_encryption_key_here
ENCRYPTION_SALT=your_16_character_salt_here

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================
# Frontend URL for redirects (e.g., email invitations)
# Local: http://localhost:3000 or http://localhost:8081
# Production: https://your-frontend-url.com
REDIRECT_URL=http://localhost:3000



# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Comma-separated list of allowed origins for CORS
# Include both localhost and 127.0.0.1 to prevent CORS issues
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8081,http://localhost:8501,http://127.0.0.1:3000,http://127.0.0.1:8081,http://127.0.0.1:8501

# =============================================================================
# EXTERNAL SERVICES (OPTIONAL)
# =============================================================================
# DDX API configuration (if using DDX integration)
DDX_API_BASE_URL=https://your-ddx-api-url.com

# =============================================================================
# STORAGE CONFIGURATION
# =============================================================================
# GCS bucket name for file storage
BUCKET_NAME=your-gcs-bucket-name

# =============================================================================
# SIGNING CREDENTIALS
# =============================================================================
# Base64 encoded service account credentials for GCS signing
# This should be the base64 encoded JSON of your service account key
SIGNING_SA_CREDENTIALS_BASE64=your_base64_encoded_service_account_json_here

# =============================================================================
# ENVIRONMENT
# =============================================================================
# Set to 'local' for local development, 'staging' or 'prod' for deployment
ENV=local
